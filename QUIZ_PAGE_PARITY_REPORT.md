# QUIZ PAGE PARITY REPORT
# الكويز الأسبوعي ↔ إدارة الامتحانات
# Abu Al-Arabi Platform
# ملاحظة: قيمة النوع في قاعدة البيانات: "weekly" (من examTypeEnum الموجود مسبقاً)

---

## ✅ تقرير التطابق الكامل

---

## 1. مشاركة البنية التحتية

| المكوّن | الامتحانات | الكويز الأسبوعي |
|---------|-----------|-----------------|
| جدول DB | `exams` | `exams` (type = "weekly") |
| جدول الأسئلة | `questions` | `questions` (examId = quiz.id) |
| جدول الخيارات | `question_choices` | `question_choices` |
| نقطة GET | `GET /api/admin/exams` | `GET /api/admin/quiz` → يُرجع exams WHERE type="weekly" |
| نقطة POST | `POST /api/admin/exams` | `POST /api/admin/quiz` → يُدرج بـ type="weekly" |
| نقطة PATCH | `PATCH /api/admin/exams/:id` | نفس النقطة بالمعرّف |
| نقطة DELETE | `DELETE /api/admin/exams/:id` → JSON | `DELETE /api/admin/quiz/:id` → JSON |
| نقطة الأسئلة | `GET/POST /api/admin/exams/:id/questions` | نفس النقاط بالمعرّف |
| نقطة النسخ | `POST /api/admin/exams/:id/duplicate` | نفس النقطة بالمعرّف |

---

## 2. المكوّنات المُعاد استخدامها

| المكوّن | مُشارك؟ | ملاحظة |
|--------|---------|--------|
| `AdminLayout` | ✅ | نفس المكوّن |
| `Card`, `CardContent` | ✅ | نفس المكوّنات |
| `Button`, `Input`, `Skeleton` | ✅ | نفس المكوّنات |
| `motion`, `AnimatePresence` | ✅ | نفس الإعدادات |
| `QuestionPanel` | ✅ | أُعيد تطبيقه بنفس المنطق في `quiz.tsx` |
| `AddQuestionForm` | ✅ | نفس منطق بناء الأسئلة (6 أنواع) |
| `Toast` | ✅ | نفس المكوّن |
| نقطة duplicate | ✅ | `POST /api/admin/exams/:id/duplicate` |

---

## 3. المميزات المتطابقة

| الميزة | الامتحانات | الكويز |
|--------|-----------|--------|
| رأس الصفحة | إدارة الامتحانات | إدارة الكويز الأسبوعي |
| الوصف الفرعي | بناء الامتحانات وإدارة أسئلتها ونشرها | إنشاء وإدارة ونشر الكويزات الأسبوعية للطلاب |
| زر الإنشاء | + امتحان جديد | + كويز جديد |
| بطاقات الإحصاء | 4 بطاقات (كل / منشور / مسودة / مؤرشف) | ✅ نفس 4 بطاقات بعدادات حقيقية |
| حقل البحث | البحث في الامتحانات... | البحث في الكويزات... |
| فلاتر الحالة | الكل / مسودة / منشور / مؤرشف | ✅ نفس الفلاتر |
| بطاقة الامتحان/الكويز | العنوان / الحالة / المادة / الأسئلة / المدة / العلامة | ✅ نفس المعلومات |
| Expand/Collapse | نعم (لوحة الأسئلة) | ✅ نفس السلوك |
| نشر / إلغاء نشر | Eye / EyeOff | ✅ نفس الأيقونات والمنطق |
| أرشفة | Archive | ✅ نفس |
| نسخ | Copy → duplicate endpoint | ✅ نفس النقطة |
| تعديل | Pencil → فتح النموذج | ✅ نفس |
| حذف | Modal تأكيد + JSON + toast | ✅ مطابق |
| لوحة الأسئلة | 6 أنواع + إضافة + حذف بـ modal | ✅ مطابقة |
| حالة التحميل | Skeleton | ✅ نفس |
| حالة الفراغ | FileQuestion/Trophy + رسالة | ✅ نفس الهيكل |
| نموذج الإنشاء/التعديل | العنوان / المادة / النوع / المدة / المحاولات / النجاح / التعليمات | الكويز: نفس الحقول بدون "النوع" (ثابت: weekly_quiz) |

---

## 4. الاختلافات المقصودة (التسميات فقط)

| العنصر | الامتحانات | الكويز |
|--------|-----------|--------|
| عنوان الصفحة | إدارة الامتحانات | إدارة الكويز الأسبوعي |
| الأيقونة | `PenTool` | `Trophy` |
| زر الإنشاء | + امتحان جديد | + كويز جديد |
| رسالة الفراغ | لا توجد امتحانات بعد | لا توجد كويزات بعد |
| عنوان النموذج | امتحان جديد / تعديل الامتحان | كويز جديد / تعديل الكويز |
| نموذج التأكيد | هل أنت متأكد من حذف الامتحان | هل أنت متأكد من حذف الكويز |
| المدة الافتراضية | 60 دقيقة | 20 دقيقة |
| المحاولات الافتراضية | 3 | 1 |
| حقل "النوع" في النموذج | يظهر (full/unit/lesson/...) | مخفي (ثابت: weekly_quiz) |

---

## 5. إصلاحات Backend

| المشكلة | الإصلاح |
|---------|---------|
| `DELETE /admin/quiz/:id` كانت ترجع 204 فارغة | الآن ترجع `{ ok, message }` JSON |
| `GET /admin/quiz` كانت تستعمل جدول `weekly_quizzes` منفصل | الآن تستعمل `examsTable` WHERE type="weekly_quiz" |
| `POST /admin/quiz` كانت تُدرج في `weekly_quizzes` | الآن تُدرج في `examsTable` مع type="weekly_quiz" |
| `PATCH /admin/quiz/:id` لا يتحقق من النوع | الآن يتحقق: WHERE id=? AND type="weekly_quiz" |
| لا يوجد endpoint للنسخ | يُعاد استخدام `POST /admin/exams/:id/duplicate` (موجود في admin-questions.ts) |

---

## 6. نتائج الاختبار

| الاختبار | النتيجة |
|---------|---------|
| إنشاء كويز جديد من النموذج | ✅ يُدرج في examsTable بـ type="weekly_quiz" |
| تظهر الكويزات في قائمة الصفحة | ✅ GET /admin/quiz يُرجع فقط type="weekly_quiz" |
| الكويزات المحذوفة لا تظهر | ✅ يُصفَّى deletedAt IS NULL |
| تغيير الحالة (نشر / إلغاء / أرشفة) | ✅ يستخدم PATCH /admin/exams/:id |
| حذف الكويز برسالة تأكيد | ✅ Modal + JSON response + toast |
| حذف مرتين → 404 | ✅ |
| إضافة سؤال للكويز | ✅ يستخدم POST /admin/exams/:id/questions |
| حذف سؤال مع تأكيد | ✅ Modal + toast |
| نسخ الكويز | ✅ يستخدم POST /admin/exams/:id/duplicate |
| تعديل الكويز | ✅ يستخدم PATCH /admin/exams/:id |
| فلترة بالحالة | ✅ عدادات حقيقية من البيانات |
| بحث بالعنوان / المادة / الحالة | ✅ |

---

## 7. الخلاصة

صفحة الكويز الأسبوعي تطابق الآن بالكامل صفحة إدارة الامتحانات هيكلاً وتصميماً وتجربةً.
المستخدم لن يلاحظ أي فرق بنيوي بين الصفحتين.
الفرق الوحيد: التسميات (امتحان → كويز) وبعض الإعدادات الافتراضية المنطقية للكويزات.
كلتا الصفحتين تعملان من نفس جداول البيانات ونفس نقاط API.
