---
name: QA Audit & Route Chain Fix 2026-07-21
description: 15 bugs fixed in prior session; full remediation brief completed in this session — 14 parts, 38/38 regression tests passing.
---

## What was done

Prior session (15 bugs):
- Health, exam/quiz route chains, ExamResult real data, nav labels, videos/flashcards removed.
- 10 student content endpoints given requireAuth.
- 28 TypeScript errors cleared.

This session (14-part remediation brief):
- Added `GET /api/admin/dossiers` with pagination, search, status, subjectId filters.
- Fixed `POST /api/admin/exams` + `POST /api/admin/quiz` — 400 if subjectId missing, 404 if subject not found.
- Replaced hardcoded `platform-stats` with 6 parallel real DB queries.
- Verified annotation isolation: every workspace query includes `userId` equality clause.
- Verified Study Room reads `dossierId`/`worksheetId` from URL params — no navigation state dependency.
- Converted all App.tsx imports to `React.lazy()` + `Suspense`; added Vite `manualChunks` by vendor.
- Updated `admin/content.tsx` to use `/api/admin/dossiers` for listing (was calling student endpoint).
- Fixed `authLimiter max` to 500 in development (15 in production) — all-localhost curl tests were tripping the 15-attempt per-15-min cap.
- Added dedicated regression test admin user: phone `0799999001`, role `admin`.
- Wrote `scripts/regression-test.sh` — 38 tests covering auth, guards, CRUD, validation, stats, annotations.

**Why:** Rate limiter is in-memory (express-rate-limit); resets on server restart. Dev tests share 127.0.0.1 IP so limit counts are shared across all curl calls. Production keeps max:15.

## Current state

- TypeScript: 0 errors
- Regression tests: 38/38 pass
- Production at malsahori.com is running OLD code — needs a new deploy to pick up all fixes
- Production DB does not have test users seeded — admin must be inserted before smoke test
- No PDF has ever been uploaded to object storage in dev environment (dossier /view returns 404 — endpoint is correct, file just doesn't exist)

## Key endpoints added / changed

- `GET /api/admin/dossiers` — new, admin-only, with pagination + filters
- `POST /api/admin/exams` — now validates subjectId → 400/404 before DB write
- `POST /api/admin/quiz` — same validation as exams
- `GET /api/dashboard/platform-stats` — real DB queries (no longer hardcoded)
- `GET /api/workspace/annotations/:dossierId/:page` — correct route signature (needs page param)
